# Dotfiles

This repository contains my personal configuration files.

## Installation

```bash
git clone https://github.com/karprabha/dotfiles.git ~/dotfiles
cd ~/dotfiles
./install.sh
```

## Structure

* `vim/` – Vim configuration and undo files

## Notes

* Vim undo history is stored in `vim/undo/`.
* Add your own configs in respective folders and update `install.sh` for symlinks.


